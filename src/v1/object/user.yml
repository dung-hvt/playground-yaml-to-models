strictProperties: true

description: User info object

type:
    - object

required:
    - name
    - fullName
    - email
    - phoneNumbers

properties:
    name:
        description: User name
        type: string

    fullName:
        description: User full name
        type: string

    email:
        description: email-ex@example.com
        type: string

    phoneNumbers:
        description: User phone-numbers
        type: array
        items:
            type: string
